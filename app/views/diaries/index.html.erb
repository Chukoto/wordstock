<h1>
  みんなの日記
</h1>

<% @diaries.each do |diary|  %>

  -----------------------
  <div>
    <div>
      <%= diary.created_at%>
    </div>

    <div>
      タイトル
      <%= diary.title %>
    </div>

    <div>
      内容
      <%= diary.text %>
    </div>
  </div>

<% end %>