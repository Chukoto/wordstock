<h1>
  <%= @content.word %>
</h1>

<div>
  <%= @content.part.name %>
</div>

<h2>
  意味
</h2>

<div>
  <% @descriptions.each do |description| %>
    <%= simple_format(description.english_text) %>
  <% end %>
</div>

<div>
  <%= link_to "意味を追加する", new_content_description_path(@content.id) %>
</div>

<h2>
  例文
</h2>

<div>
  <%= link_to "例文を追加する", new_content_sentence_path(@content.id) %>
</div>